import { Button } from "@/components/ui/button";

const Home = () => {
  return (
    <div>
      <Button size={"lg"} variant={"destructive"}>
        Click me
      </Button>
    </div>
  );
};

export default Home;
